# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

jobs:
- job: echo "job1"
  displayName: echo "first job"

  steps:
  - script: echo "First step"
    displayName: First step desc...

  - script: |
      echo "Second step"
      echo "2 step"
    displayName: Second step desc...